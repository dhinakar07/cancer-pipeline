# ============================================================
# cancer-pipeline — Python dependencies
# Author: dhinakaryalla07@gmail.com
# Stack : Python, PostgreSQL, Apache Airflow, Streamlit
# ============================================================

# ── Database ────────────────────────────────────────────────
psycopg2-binary==2.9.9          # PostgreSQL adapter
SQLAlchemy==2.0.30              # ORM / connection pooling

# ── Data processing ─────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2             # preprocessing utilities

# ── Deep learning (Bi-LSTM model) ───────────────────────────
tensorflow==2.16.1              # loads the saved Keras .h5 model
keras==3.3.3

# ── NLP / text preprocessing ────────────────────────────────
nltk==3.8.1
regex==2024.4.16

# ── Apache Airflow ──────────────────────────────────────────
apache-airflow==2.9.1
apache-airflow-providers-postgres==5.10.2

# ── Streamlit dashboard ─────────────────────────────────────
streamlit==1.35.0
plotly==5.22.0
altair==5.3.0

# ── Kaggle API (dataset download) ───────────────────────────
kaggle==1.6.14

# ── AWS SDK ─────────────────────────────────────────────────
boto3==1.34.101                 # AWS S3, RDS, IAM interactions
botocore==1.34.101              # boto3 dependency (pinned for stability)

# ── Utilities ───────────────────────────────────────────────
python-dotenv==1.0.1            # load .env variables
loguru==0.7.2                   # structured logging
tqdm==4.66.4                    # progress bars
pytest==8.2.1                   # unit testing
pytest-cov==5.0.0               # coverage reports
